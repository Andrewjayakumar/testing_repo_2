import { VidPreviewDirective } from './vid-preview.directive';

describe('VidPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new VidPreviewDirective();
    expect(directive).toBeTruthy();
  });
});

import { FunnelChartDirective } from './funnel-chart.directive';

describe('FunnelChartDirective', () => {
  it('should create an instance', () => {
    const directive = new FunnelChartDirective();
    expect(directive).toBeTruthy();
  });
});
